# 智能引导功能 — 接口文档

> **Base URL**：`http://{host}:8200`
> **版本**：v3
> **统一响应格式**：`{"code": 0, "message": "操作成功", "data": {...}}` — `code=0` 成功，`code=-1` 失败
> 最后更新：2026-07-22
>
> **v3 变更**：`/search` 改为纯意图分流器（qa 意图只返回标识）；知识问答独立为 `/qa/stream`（SSE 流式）；新增 `recent_projects` 意图；会话表增加 `conversation_type` 区分 `project`/`digital_human`。
> **范围**：本文档仅保留 `static/绩效平台首页.html` 实际调用的 9 个接口。已删除未使用端点（`/intent`、非流式 `/qa`、`/assistant/project/stream`、会话增删、`/assistant/projects`、`/page-guide`、`/routes`、`/stages`、`/project/getById`）。

---

## 目录

**前端首页使用的接口**
- [1. 近期项目列表](#1-近期项目列表)
- [2. 待办项目列表](#2-待办项目列表)
- [3. 全局搜索（意图分流）](#3-全局搜索意图分流)
- [4. 知识问答（流式）](#4-知识问答sse-流式)
- [5. 智能问数跳转](#5-智能问数跳转)
- [6. 项目助手问答](#6-项目助手问答)
- [7. 项目助手 · 会话管理](#7-项目助手--会话管理)
- [8. 政策助手跳转](#8-政策助手跳转)

**附录**
- [附录 A：taskType 编码](#附录-ataskType-编码)
- [附录 B：项目助手阶段编码](#附录-B项目助手阶段编码)
- [附录 C：前端对接协议](#附录-C前端对接协议)

---

## 1. 近期项目列表

用户近期参与的绩效项目列表，数据来自 `getProjectCommissionTaskHome` 外部 API。

### 请求

```
GET /api/v1/guide/projects/recent?page=1&rows=6
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码，默认 1 |
| `rows` | int | 否 | 每页条数，默认 6 |

> **认证说明**：通过转发请求头中的 `Uapaccesstoken` 到外部绩效系统 API 完成鉴权。
> **ID 精度**：所有大整数 ID（如 `commissionTaskId`）均以**字符串**返回，避免 JS Number 精度丢失。

### 响应

```json
{
  "code": 0,
  "message": "获取近期项目列表成功",
  "data": {
    "projects": [
      {
        "todoId": "home_xxx",
        "type": "commission_task",
        "title": "2024年度绩效自评报告",
        "projectName": "XX基金绩效评价",
        "commissionTaskId": "2079773087175282688",
        "taskType": 2,
        "taskTypeName": "事后评价",
        "currentStage": "report",
        "currentStageName": "报告撰写",
        "deadline": "2026-07-15",
        "startDate": "2026-06-01",
        "status": "in_progress",
        "statusText": "进行中",
        "source": "project_home",
        "cardActions": ["qa", "query", "report", "review"]
      }
    ],
    "total": 35,
    "page": 1,
    "rows": 6,
    "source": "project_home"
  }
}
```

> **分页说明**：`total` 是外部 API 返回的总数，前端据此渲染页码按钮。`commissionTaskId` 为字符串格式。

### 数据来源

| 来源 | 外部 API | 方法 | 说明 |
|------|---------|------|------|
| `project_home` | `{PERFORMANCE_SYSTEM_URL}/jx/web/api/v1/projectCommissionTask/getProjectCommissionTaskHome` | POST | 评价任务首页列表 |

---

## 2. 待办项目列表

用户待处理的 BPM 流程任务列表，数据来自 `inboxTasks` 外部 API。

### 请求

```
GET /api/v1/guide/projects/todos
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 无 | — | — | 用户身份从请求头 `Uapaccesstoken` 自动获取 |

### 响应

```json
{
  "code": 0,
  "message": "获取待办项目列表成功",
  "data": {
    "projects": [
      {
        "todo_id": "inbox_xxx",
        "type": "bpm_task",
        "title": "XX项目审批",
        "project_name": "XX项目审批",
        "commission_task_id": "2026906953246052352",
        "task_type": 2,
        "task_type_name": "事后评价",
        "current_stage": "project",
        "current_stage_name": "评价任务列表",
        "deadline": "",
        "start_date": "2026-06-15",
        "status": "in_progress",
        "status_text": "待处理",
        "source": "inbox",
        "bpm_task_id": "xxx",
        "assignee": "审批人ID",
        "initiator_name": "发起人",
        "process_definition_key": "xxx",
        "duration_desc": "2小时"
      }
    ],
    "total": 3,
    "source": "inbox"
  }
}
```

### 数据来源

| 来源 | 外部 API | 方法 | 说明 |
|------|---------|------|------|
| `inbox` | `{PERFORMANCE_SYSTEM_URL}/bpmc/action/task/inboxTasks` | POST | BPM 待办收件箱 |

> **分页**：支持 `?page=1&rows=20`，"全部"分类做后端分页，其余分类整组返回。
> **分类**：按 `processDefinitionKey` + `businessPattern` 归类为 绩效目标申报/事前/事中/事后，非绩效任务被过滤。

### 错误响应

```json
{
  "code": -1,
  "message": "未获取到用户信息，返回空列表",
  "data": null
}
```

---

## 3. 全局搜索（意图分流）

自然语言输入 → 意图识别 → 返回分流结果。采用分层识别策略（近期项目/数据查询预检 → 关键词匹配 → LLM 意图识别），LLM 不可用时自动降级。

> **架构变更（v3）**：`/search` 现在是**纯意图分流器**——只做分类不生成回答。非 qa 意图（navigation/recent_projects/query/policy）会**落库会话历史并返回 conversation_id**；qa 意图只返回标识，由前端调 `/qa/stream` 独立完成问答与落库。

### 请求

```
POST /api/v1/guide/search
Content-Type: application/json
```

```json
{
  "query": "我想填报绩效目标",
  "user_id": "1696097681761374208",
  "conversation_id": "",
  "request_id": ""
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `query` | string | **是** | 用户自然语言输入 |
| `user_id` | string | 否 | 用户 ID |
| `conversation_id` | string | 否 | 当前 digital_human 会话 ID，传入则续接该会话 |
| `request_id` | string | 否 | 幂等 ID，服务端自动生成 |

### 响应 — 页面导航

当识别为用户想跳转到某个页面时返回。**非 qa 意图会落库会话并返回 conversation_id**。

```json
{
  "code": 0,
  "message": "意图识别成功",
  "data": {
    "intent": "navigation",
    "sub_intent": "target_task_list",
    "confidence": 0.95,
    "route": {
      "id": "target_list",
      "title": "目标申报评价任务列表",
      "url": "/performance/commission/target",
      "params": {}
    },
    "guide_stage": "target_list",
    "description": "为您跳转到目标申报评价任务列表页",
    "conversation_id": "1234567890123456789",
    "request_id": "req-abc123"
  }
}
```

> **`guide_stage`**：前端可根据此值判断应启动哪个业务类型的操作指引场景。值为 `null` 时表示该页面暂无操作指引场景。

### 响应 — 近期项目

v3 新增意图。非 qa 意图，落库会话并返回 `conversation_id`。

```json
{
  "code": 0,
  "message": "检测为近期项目查询意图",
  "data": {
    "intent": "recent_projects",
    "sub_intent": "recent_projects",
    "confidence": 0.9,
    "description": "为您展示近期参与的绩效项目列表。",
    "action": "show_recent_projects",
    "conversation_id": "1234567890123456789"
  }
}
```

> **前端行为**：收到此意图后直接调用 `openRecentProjects()` 弹出近期项目列表。

### 响应 — 知识问答

**仅返回标识，不含答案**。前端收到后调用 `POST /qa/stream` 取得流式回答。**不落库会话**（由 `/qa/stream` 负责落库）。

```json
{
  "code": 0,
  "message": "检测为知识问答意图，请使用 /qa 接口",
  "data": {
    "intent": "qa",
    "sub_intent": "general",
    "confidence": 0.88,
    "description": "检测为知识问答，请调用 /qa 接口获取回答",
    "action": "call_qa_api"
  }
}
```

> **前端行为**：收到 `action: "call_qa_api"` 后调用 `POST /qa/stream` 获取流式回答（见 §4）。

### 响应 — 数据查询意图

非 qa 意图，落库会话。

```json
{
  "code": 0,
  "message": "检测为数据查询意图，请使用智能问数系统",
  "data": {
    "intent": "query",
    "sub_intent": "smart_query",
    "confidence": 0.85,
    "description": "数据查询请使用项目卡片上的 [问数] 按钮，将跳转到智能问数系统。",
    "action": "redirect_to_smart_query",
    "conversation_id": "1234567890123456789"
  }
}
```

### 响应 — 政策咨询意图

非 qa 意图，落库会话。

```json
{
  "code": 0,
  "message": "检测为政策咨询意图，请使用政策助手系统",
  "data": {
    "intent": "policy",
    "sub_intent": "policy_inquiry",
    "confidence": 0.82,
    "description": "政策法规咨询请使用 [政策助手] 按钮，将跳转到政策法规问答系统。",
    "action": "redirect_to_policy_qa",
    "conversation_id": "1234567890123456789"
  }
}
```

### 意图识别流程

```
用户输入
    │
    ▼
① 近期项目预检（关键词："近期项目/我参与的项目/我的项目"…）
    ├── 命中 → recent_projects → 落库 → 返回
    └── 未命中 ↓
② 数据查询预检（"多少钱/总额/统计" + 业务关键词，排除"什么是/定义"等QA句式）
    ├── 命中 → query → 落库 → 返回
    └── 未命中 ↓
③ 关键词匹配（route 注册表）
    ├── 置信度 ≥0.8 → navigation → 落库 → 返回
    └── 未达标 ↓
④ LLM 意图识别
    ├── navigation → 匹配路由 → 落库 → 返回
    ├── recent_projects → 落库 → 返回
    ├── query → 落库 → 返回
    ├── policy → 落库 → 返回
    ├── qa → 只返回标识，不落库（由 /qa/stream 负责）
    └── LLM 不可用 → 降级处理
```

### `intent` 取值说明

| intent | 说明 | 是否落库 |
|--------|------|----------|
| `navigation` | 页面导航，含 `route` + `guide_stage` | ✅ 落库 digital_human 会话 |
| `recent_projects` | 查看近期项目，前端弹窗 | ✅ 落库 digital_human 会话 |
| `query` | 数据查询，引导使用问数按钮 | ✅ 落库 digital_human 会话 |
| `policy` | 政策咨询，引导使用政策助手 | ✅ 落库 digital_human 会话 |
| `qa` | 知识问答，前端调 `/qa/stream` | ❌ 不落库，由 `/qa/stream` 负责 |

### 全局搜索联动操作指引

当意图为 `navigation` 时，响应中 `guide_stage` 字段指示前端应启动哪个业务类型的操作指引预制流程：

| guide_stage | 对应操作指引场景 | 步骤数 |
|-------------|----------------|--------|
| `target_list` | 绩效目标申报完整流程 | 15 步 |
| `before_list` | 事前评估完整流程 | 16 步 |
| `middle_list` | 事中监控完整流程 | 17 步 |
| `after_list` | 事后评价完整流程 | 16 步 |

---

## 4. 知识问答（SSE 流式）

前端收到 `/search` 返回的 `qa` 标识后调用。**负责落库 digital_human 会话历史**。

```
POST /api/v1/guide/qa/stream
Content-Type: application/json
```

```json
{
  "query": "什么是预算绩效管理",
  "user_id": "1696097681761374208",
  "conversation_id": "",
  "request_id": ""
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `query` | string | **是** | 用户问题 |
| `user_id` | string | 否 | 用户 ID |
| `conversation_id` | string | 否 | 续接已有 digital_human 会话；为空则新建 |
| `request_id` | string | 否 | 幂等 ID |

响应为 **SSE 流式**（`text/event-stream`）。

**事件协议**：

| event | data | 说明 |
|-------|------|------|
| `meta` | `{"conversation_id","turn_no","replayed"}` | 会话元信息 |
| `delta` | `{"content":"预"}` → `{"content":"算"}` … | 逐字增量，可多次 |
| `done` | `{"conversation_id","request_id","answer","source"}` | 完成 |
| `error` | `{"message":"…"}` | 异常 |

**前端对接示例**：

```javascript
fetch('/api/v1/guide/qa/stream', {
  method: 'POST',
  headers: {'Content-Type':'application/json'},
  body: JSON.stringify({query, user_id, conversation_id, request_id})
}).then(async res => {
  const reader = res.body.getReader();
  const decoder = new TextDecoder();
  let buf = '', answer = '';
  while (true) {
    const {done, value} = await reader.read();
    if (done) break;
    buf += decoder.decode(value, {stream:true});
    // 解析 SSE 行: event:delta\ndata:{"content":"xxx"}
    // 逐行追加 answer 并渲染气泡
    const lines = buf.split('\n');
    buf = lines.pop();
    for (const line of lines) {
      if (line.startsWith('data:')) {
        const data = JSON.parse(line.slice(5).trim());
        if (data.content) answer += data.content;  // delta
        if (data.conversation_id) saveCid(data.conversation_id); // done
      }
    }
    updateBubble(escapeHtml(answer));
  }
});
```

---

## 5. 智能问数跳转

构造外部智能问数系统的跳转 URL，携带项目上下文参数。**本接口仅返回跳转 URL，不承载数据查询逻辑。**

### 请求

```
POST /api/v1/guide/query/redirect
Content-Type: application/json
```

```json
{
  "commission_task_id": "2026906953246052352",
  "query": "完成了多少产出指标？",
  "user_id": "1696097681761374208"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `commission_task_id` | string | **是** | 评价任务 ID |
| `query` | string | 否 | 用户查询问题（透传到外部系统） |
| `user_id` | string | 否 | 用户 ID |

### 响应

```json
{
  "code": 0,
  "message": "智能问数跳转链接",
  "data": {
    "redirect_url": "https://smart-query.example.com?commissionTaskId=2026906953246052352&taskName=XX基金绩效评价&taskType=2&userId=1696097681761374208&query=完成了多少产出指标？",
    "external_system_name": "智能问数系统",
    "open_in_new_tab": true
  }
}
```

### 携带参数

| 参数 | 来源 | 说明 |
|------|------|------|
| `commissionTaskId` | 请求参数 | 评价任务 ID |
| `taskName` | 达梦数据库查询 | 任务名称（从 `b_project_commission_task` 表获取） |
| `taskType` | 达梦数据库查询 | 任务类型（1/2/3/5） |
| `userId` | 请求参数 | 用户 ID |
| `query` | 请求参数（可选） | 用户查询问题 |

### 前置条件

- 环境变量 `SMART_QUERY_SYSTEM_URL` 必须配置，否则返回：`"智能问数系统暂未配置，请联系管理员"`

### 前端使用

```javascript
const res = await fetch('/api/v1/guide/query/redirect', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ commission_task_id: 'xxx', user_id: 'xxx' })
});
const { data } = await res.json();
if (data.redirect_url) {
  window.open(data.redirect_url, '_blank');
}
```

---

## 6. 项目助手问答

根据用户选择的四阶段之一，结合项目全量上下文，调用 LLM 生成深度回答。支持多轮会话与历史记录。

### 请求

```
POST /api/v1/guide/assistant/project
Content-Type: application/json
```

```json
{
  "stage": "post_evaluation",
  "commission_task_id": "2079773087175282688",
  "project_name": "XX基金绩效评价",
  "query": "如何撰写绩效自评报告？",
  "user_id": "1696097681761374208",
  "conversation_id": "",
  "request_id": ""
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `stage` | string | **是** | 阶段编码（见附录 B） |
| `commission_task_id` | string | **是** | 评价任务 ID（字符串，避免 JS 精度丢失） |
| `project_name` | string | 否 | 项目名称，新会话时作为会话标题 |
| `query` | string | **是** | 用户问题 |
| `user_id` | string | 否 | 用户 ID |
| `conversation_id` | string | 否 | 续接已有**项目会话**（project 类型）；为空则新建 |
| `request_id` | string | 否 | 幂等 ID |

> **会话隔离**：传入 `conversation_id` 时会校验归属、类型（必须为 `project`）、项目 ID 和阶段一致性，不匹配直接返回错误。

> **流式版本**：`POST /api/v1/guide/assistant/project/stream`，请求体相同，响应为 SSE 流式（事件协议同 §4）。

### 响应

```json
{
  "code": 0,
  "message": "项目助手回答成功",
  "data": {
    "stage": "post_evaluation",
    "stage_name": "事后评价",
    "commission_task_id": "2026906953246052352",
    "answer": "撰写绩效自评报告时，建议按以下结构组织内容：\n\n## 一、项目基本情况\n...",
    "references": [],
    "suggestions": [
      "绩效自评报告撰写指南",
      "如何对比目标与实际完成情况？",
      "项目经验总结与改进建议"
    ],
    "source": "llm"
  }
}
```

| 字段 | 说明 |
|------|------|
| `answer` | LLM 生成的回答（Markdown 格式） |
| `references` | 知识库引用（预留） |
| `suggestions` | 建议追问列表（按阶段预设） |
| `source` | `"llm"` 正常生成，`"fallback"` LLM 不可用时的降级消息 |

### 降级场景

LLM 调用失败时，`source` 为 `"fallback"`，返回基本提示信息：

```json
{
  "answer": "[降级] 您正在事后评价阶段查询项目 2026906953246052352。完整的智能回答将在 LLM 恢复后提供。",
  "source": "fallback"
}
```

### 项目上下文查询范围（按阶段加载）

项目助手按阶段加载不同的项目数据模块作为 LLM 上下文（`STAGE_MODULES`），减少 token 消耗。

**公共模块**（所有阶段）：辅助配置、项目大组、评价子任务、评价点位、指标评分、问题汇总、评价要点、薄弱环节。

| 阶段 | 额外加载模块 |
|------|-------------|
| `target_declaration` 目标申报 | 下达指标、评价指标目标、评价要求 |
| `pre_evaluation` 事前评估 | 下达指标、方案任务、报告任务、报告 |
| `mid_monitoring` 事中监控 | 监控单位、绩效分析、项目绩效、日报、周报、重大事项 |
| `post_evaluation` 事后评价 | 项目执行指标、评价统计、报告任务、报告、项目关联、总结内容 |

> 每个列表模块最多展示前 10 条，超出部分截断标注 `... 共 N 条（以上展示前 10 条）`。
> **ID 与字段名脱敏**：回答后处理会移除数据库字段名和数字 ID，统一替换为中文标签或"该项目/该点位"。

### 阶段系统提示词

每个阶段有专用的 LLM 系统提示词，详见下表：

| 阶段 | 提示词侧重 |
|------|----------|
| `target_declaration` | 指标设置规范、申报完整性检查、同类参考、可量化指标设定 |
| `pre_evaluation` | 政策解读、事前报告编制、立项必要性、预算合理性、可行性论证 |
| `mid_monitoring` | 进度偏差分析、关键节点提醒、偏离预警、监控报告填写、预算执行率 |
| `post_evaluation` | 自评报告撰写、目标vs实际对比、经验总结、指标体系解读、改进建议 |

### 各阶段建议追问

| 阶段 | 建议追问 |
|------|---------|
| `target_declaration` | "如何设置合理的绩效指标？" / "绩效目标申报需要哪些材料？" / "同类型项目的指标参考" |
| `pre_evaluation` | "事前评估报告如何撰写？" / "项目立项必要性分析要点" / "预算合理性评估方法" |
| `mid_monitoring` | "当前项目执行进度如何？" / "如何识别绩效目标偏离风险？" / "绩效监控报告填写要点" |
| `post_evaluation` | "绩效自评报告撰写指南" / "如何对比目标与实际完成情况？" / "项目经验总结与改进建议" |

---

## 7. 项目助手 · 会话管理

> 前端首页仅使用**会话列表**与**会话消息**两个只读接口用于历史记录回放。

### 7.1 会话列表

```
GET /api/v1/guide/assistant/conversations?user_id=xxx&conversation_type=&limit=50
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `user_id` | string | 否 | 用户 ID（后端从 token 解析） |
| `conversation_type` | string | 否 | 过滤类型：`project` / `digital_human`；不传返回全部 |
| `limit` | int | 否 | 每页条数，默认 50 |

**响应**：

```json
{
  "code": 0,
  "data": {
    "conversations": [
      {
        "id": "1234567890123456789",
        "userId": "1696097681761374208",
        "conversationType": "project",
        "commissionTaskId": "2079773087175282688",
        "taskType": 2,
        "stage": "post_evaluation",
        "title": "XX基金绩效评价",
        "conversationStatus": "active",
        "messageCount": 6,
        "lastMessagePreview": "撰写自评报告时，建议按以下结构…",
        "lastMessageAt": "2026-07-22 15:30:00",
        "createdAt": "2026-07-22 10:00:00",
        "updatedAt": "2026-07-22 15:30:00"
      }
    ],
    "total": 5
  }
}
```

> **`conversationType`**：`project` = 项目问答，`digital_human` = 普通数字人问答。前端据此渲染类型标签，恢复会话时选择对应发送接口。

### 7.2 会话消息

```
GET /api/v1/guide/assistant/conversation/{conversation_id}/messages?user_id=xxx
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `user_id` | string | 否 | **归属校验**：仅返回属于该用户的会话消息，不匹配返回 403 |

**响应**：

```json
{
  "code": 0,
  "data": {
    "conversation": {"id":"…","conversationType":"project","title":"…"},
    "messages": [
      {
        "id": "msg-1",
        "seqNo": 1,
        "turnNo": 1,
        "role": "USER",
        "content": "如何撰写绩效自评报告？",
        "messageStatus": "COMPLETED",
        "createdAt": "2026-07-22 15:30:00"
      },
      {
        "id": "msg-2",
        "seqNo": 2,
        "turnNo": 1,
        "role": "ASSISTANT",
        "content": "撰写自评报告时，建议按以下结构…",
        "messageStatus": "COMPLETED",
        "answerSource": "LLM",
        "createdAt": "2026-07-22 15:30:05"
      }
    ],
    "total": 2
  }
}
```

> **前端恢复会话**：根据 `conversationType` 恢复 `currentConversationState` —— project 走 `/assistant/project`，digital_human 走 `/search`+`/qa/stream`。

---

## 8. 政策助手跳转

构造外部政策法规问答系统的跳转 URL，携带用户身份参数。**本接口仅返回跳转 URL，不承载政策问答逻辑。**

### 请求

```
POST /api/v1/guide/assistant/policy/redirect
Content-Type: application/json
```

```json
{
  "query": "绩效评价的最新政策有哪些？",
  "user_id": "1696097681761374208"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `query` | string | 否 | 用户政策问题（预留透传） |
| `user_id` | string | 否 | 用户 ID |

### 响应

```json
{
  "code": 0,
  "message": "政策助手跳转链接",
  "data": {
    "redirect_url": "https://policy-qa.example.com?userId=1696097681761374208",
    "external_system_name": "政策法规问答系统",
    "open_in_new_tab": true
  }
}
```

### 携带参数

跳转到外部政策问答系统时，通过 URL query 参数传递用户上下文：

| 参数 | 说明 | 示例 | 当前实现状态 |
|------|------|------|-------------|
| `userId` | 当前登录用户 ID | `1696097681761374208` | ✅ 已实现 |
| `tenantId` | 租户 ID | `xxx` | ⏳ 待实现（需从请求头提取） |

> **待办**：`tenantId` 当前未实现。产品文档定义需携带该参数，但代码 `guide.py` 中的 `policy_assistant_redirect` 仅传递了 `userId`。需从 `request.headers` 中提取 `tenantid` 拼接到跳转 URL。

### 前置条件

- 环境变量 `POLICY_QA_SYSTEM_URL` 必须配置，否则返回：`"政策问答系统暂未配置，请联系管理员"`

### 与全局搜索「知识问答」的区别

| 对比维度 | 政策助手（本接口） | 全局搜索-知识问答 |
|----------|-------------------|-------------------|
| 触发方式 | 主动点击按钮 → 新窗口打开外部系统 | 在搜索框输入问题 |
| 知识范围 | 聚焦政策法规（外部独立系统） | 全域知识库（含操作指引、流程等） |
| 交互深度 | 外部系统独立对话 | 单轮问答为主 |
| 上下文 | 不绑定项目 | 可绑定页面上下文 |

## 附录

### 附录 A：taskType 编码

| 编码 | 名称 | 说明 |
|------|------|------|
| `1` | 事前评估 | 项目启动前的绩效评估 |
| `2` | 事后评价 | 项目完成后的绩效评价 |
| `3` | 目标申报 | 项目立项时的绩效目标申报 |
| `5` | 事中监控 | 项目执行中的绩效监控 |

> ⚠ 编码 `4` 为历史保留值，不作业务使用。

### 附录 B：项目助手阶段编码

项目助手问答（§6）与获取阶段项目列表接口使用的四阶段编码：

| 编码 | 名称 | 对应 taskType |
|------|------|-------------|
| `target_declaration` | 绩效目标申报 | 3 |
| `pre_evaluation` | 事前评价 | 1 |
| `mid_monitoring` | 事中监控 | 5 |
| `post_evaluation` | 事后评价 | 2 |

---

## 接口速查表

| # | 方法 | 路径 | 说明 |
|---|------|------|------|
| 1 | GET | `/api/v1/guide/projects/recent` | 近期项目列表（分页） |
| 2 | GET | `/api/v1/guide/projects/todos` | 待办项目列表 |
| 3 | POST | `/api/v1/guide/search` | 全局搜索（意图分流 + 落库） |
| 4 | POST | `/api/v1/guide/qa/stream` | 知识问答（SSE 流式） |
| 5 | POST | `/api/v1/guide/query/redirect` | 智能问数跳转 |
| 6 | POST | `/api/v1/guide/assistant/project` | 项目助手问答 |
| 7 | POST | `/api/v1/guide/assistant/policy/redirect` | 政策助手跳转 |
| 8 | GET | `/api/v1/guide/assistant/conversations` | 会话列表 |
| 9 | GET | `/api/v1/guide/assistant/conversation/{id}/messages` | 会话消息 |

> 共 9 个接口，均为 `static/绩效平台首页.html` 实际调用。已删除未使用端点：`/intent`、`/qa`(非流式)、`/assistant/project/stream`、`/assistant/conversation/create`、`/assistant/conversation/delete`、`/assistant/projects`、`/page-guide`、`/routes`、`/stages`、`/project/getById`。

---

## 附录 C：前端对接协议

### E.1 会话状态模型

前端维护单一 `currentConversationState`，包含：

| 字段 | 类型 | 说明 |
|------|------|------|
| `conversationType` | `'digital_human'` / `'project'` | 当前会话类型 |
| `conversationId` | string | 持久化会话 ID，首次提问为空 |
| `commissionTaskId` | string | 仅 project 类型 |
| `taskType` | string | 仅 project 类型 |
| `stage` | string | 仅 project 类型 |
| `taskName` | string | 仅 project 类型 |

### E.2 发送路由

```
用户输入
    │
    ├─ conversationType === 'project'
    │   └─ POST /assistant/project
    │      （带 stage/commission_task_id/project_name/conversation_id/request_id）
    │      返回 answer 整段渲染
    │
    └─ conversationType === 'digital_human'
        └─ POST /search
           （带 query/conversation_id/request_id）
           ├─ navigation  → renderSearchResult（路由卡片）
           ├─ recent_projects → openRecentProjects()
           ├─ query/policy → 引导按钮
           └─ qa → POST /qa/stream
                  （流式渲染，保存 conversation_id）
```

### E.3 入口切换

| 操作 | 行为 |
|------|------|
| 点击项目卡片"提问" | `conversationType='project'`, `conversationId=''`, 打开面板 |
| 点击"新对话" | `conversationType='digital_human'`, `conversationId=''`, 清空聊天 |
| 点击"×"移除项目引用 | 同"新对话" |
| 恢复历史会话 | 按 `conversationType` 恢复状态，project 走 `/assistant/project`，digital_human 走 `/search` |

### E.4 SSE 流式接收示例

```javascript
async function streamQa(query, conversationId) {
  const res = await fetch('/api/v1/guide/qa/stream', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({query, user_id:UID, conversation_id:conversationId, request_id:genReqId()})
  });
  const reader = res.body.getReader(), decoder = new TextDecoder();
  let buf = '', answer = '';
  while (true) {
    const {done, value} = await reader.read();
    if (done) break;
    buf += decoder.decode(value, {stream: true});
    const lines = buf.split('\n');
    buf = lines.pop();
    for (const line of lines) {
      const m = line.match(/^event:\s*(\w+)/);
      const d = line.match(/^data:\s*(.+)/);
      if (!d) continue;
      const data = JSON.parse(d[1]);
      if (m && m[1] === 'delta' && data.content) {
        answer += data.content;
        updateBubble(answer);  // 逐字渲染
      }
      if (m && m[1] === 'done') {
        saveConversationId(data.conversation_id);
      }
    }
  }
}
```
