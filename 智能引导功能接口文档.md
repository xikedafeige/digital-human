# 智能引导功能 — 接口文档

> **Base URL**：`http://{host}:8200`
> **版本**：v10
> **统一响应格式**：`{"code": 0, "message": "操作成功", "data": {...}}` — `code=0` 成功，`code=-1` 失败
> 最后更新：2026-08-03
>
> **v10 变更**：**文件查找改为「文件名反查·一步出文件」**——§10 `/file/reverse-locate` 输入文件名，后端按 `KN_INFO.TITLE` 反查所属绩效任务、用 `source_id(=commission_task_id)` 走 pageList **权限过滤**，直接返回过滤后的文件列表（每个文件带所属任务名），用户不再先选任务。废弃 v9 的三步接口 `/file/locate-task`、`/file/locate-dir`、`/file/search`（后端 `/file/search` 保留未删，供指定任务复用）。
> **v9 新增**：**文件查找**功能——§3 `/search` 新增 `file_query` 意图（"我想查找xxx文件"/"查xxx文件内容"，拆出完整文件名）。知识库表在达梦实例 `langwell_doc`（schema `LANGWELL-DOC`）。
>
> **v8 变更**：智能问数**前端改为跳转外部系统**——§5 `/query/redirect` 扩展为单项目 + 跨项目统一入口（`commission_task_id` 改可选，新增 `project_ids`），前端【问数】按钮跳转到外部智能问数系统（展示层）。**外部系统的数据查询与大模型分析仍由本后端 §5.1 `/query/analyze` 提供**（跨项目 SSE 分析接口保留，未删除）。原「数据查询任务卡片 `/query/projects`」顺延为 §5.2。
>
> **v3 变更**：`/search` 改为纯意图分流器（qa 意图只返回标识）；知识问答独立为 `/qa/stream`（SSE 流式）；新增 `recent_projects` 意图；会话表增加 `conversation_type` 区分 `project`/`digital_human`。
> **v4 新增**：`/query/analyze` 跨项目智能问数分析（系统内查数据+分析），SSE 流式、支持多轮追问、下钻子项目聚合、ECharts 图表输出。
> **v5 新增**：`/assistant/project/stream` 项目助手 SSE 流式版（前端首页默认调用），与非流式 `/assistant/project` 逻辑一致，逐字增量推送回答。
> **v6 变更**：`/search` 新增 `cooperation`（协同操作手册）与 `disambiguation`（意图歧义二次追问）意图，请求新增 `chosen_*` 点选回传字段；**修正 `query` 意图不落库**（与 qa 一致，避免共享 request_id 触发幂等重放）；`/cooperation/search` 协同指引检索（§9）。
> **v7 变更**：`query`（数据查询）意图**改为直接返回绩效任务卡片数据、不经大模型分析**——前端命中 `query` 后调用新增的 `POST /query/projects`（§5.2，定位任务+算关键指标，普通 JSON），渲染成项目卡片。
> **范围**：本文档保留前端实际调用的接口。已删除未使用端点（`/intent`、非流式 `/qa`、会话增删、`/assistant/projects`、`/page-guide`、`/routes`、`/stages`、`/project/getById`）。

---

## 目录

**前端首页使用的接口**
- [1. 近期项目列表](#1-近期项目列表)
- [2. 待办项目列表](#2-待办项目列表)
- [3. 全局搜索（意图分流）](#3-全局搜索意图分流)
- [4. 知识问答（流式）](#4-知识问答sse-流式)
- [5. 智能问数跳转](#5-智能问数跳转)
- [5.1 跨项目智能问数分析（SSE）](#51-跨项目智能问数分析sse-流式)
- [5.2 数据查询任务卡片（/query/projects）](#52-数据查询任务卡片query-projects)
- [6. 项目助手问答](#6-项目助手问答)
- [7. 项目助手 · 会话管理](#7-项目助手--会话管理)
- [8. 政策助手跳转](#8-政策助手跳转)
- [9. 协同指引搜索](#9-协同指引搜索)
- [10. 文件查找 · 文件名反查（一步出文件）](#10-文件查找--文件名反查一步出文件)

**附录**
- [附录 A：taskType 编码](#附录-ataskType-编码)
- [附录 B：项目助手阶段编码](#附录-B项目助手阶段编码)
- [附录 C：前端对接协议](#附录-C前端对接协议)

---

## 1. 近期项目列表

用户近期参与的绩效项目列表，数据来自 `getProjectCommissionTaskHome` 外部 API。

### 请求

```
GET /api/v1/guide/projects/recent?page=1&rows=6
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码，默认 1 |
| `rows` | int | 否 | 每页条数，默认 6 |

> **认证说明**：通过转发请求头中的 `Uapaccesstoken` 到外部绩效系统 API 完成鉴权。
> **ID 精度**：所有大整数 ID（如 `commissionTaskId`）均以**字符串**返回，避免 JS Number 精度丢失。

### 响应

```json
{
  "code": 0,
  "message": "获取近期项目列表成功",
  "data": {
    "projects": [
      {
        "todoId": "home_xxx",
        "type": "commission_task",
        "title": "2024年度绩效自评报告",
        "projectName": "XX基金绩效评价",
        "commissionTaskId": "2079773087175282688",
        "taskType": 2,
        "taskTypeName": "事后评价",
        "currentStage": "report",
        "currentStageName": "报告撰写",
        "deadline": "2026-07-15",
        "startDate": "2026-06-01",
        "status": "in_progress",
        "statusText": "进行中",
        "source": "project_home",
        "cardActions": ["qa", "query", "report", "review"]
      }
    ],
    "total": 35,
    "page": 1,
    "rows": 6,
    "source": "project_home"
  }
}
```

> **分页说明**：`total` 是外部 API 返回的总数，前端据此渲染页码按钮。`commissionTaskId` 为字符串格式。

### 数据来源

| 来源 | 外部 API | 方法 | 说明 |
|------|---------|------|------|
| `project_home` | `{PERFORMANCE_SYSTEM_URL}/jx/web/api/v1/projectCommissionTask/getProjectCommissionTaskHome` | POST | 评价任务首页列表 |

---

## 2. 待办项目列表

用户待处理的 BPM 流程任务列表，数据来自 `inboxTasks` 外部 API。

### 请求

```
GET /api/v1/guide/projects/todos
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 无 | — | — | 用户身份从请求头 `Uapaccesstoken` 自动获取 |

### 响应

```json
{
  "code": 0,
  "message": "获取待办项目列表成功",
  "data": {
    "projects": [
      {
        "todo_id": "inbox_xxx",
        "type": "bpm_task",
        "title": "XX项目审批",
        "project_name": "XX项目审批",
        "commission_task_id": "2026906953246052352",
        "task_type": 2,
        "task_type_name": "事后评价",
        "current_stage": "project",
        "current_stage_name": "评价任务列表",
        "deadline": "",
        "start_date": "2026-06-15",
        "status": "in_progress",
        "status_text": "待处理",
        "source": "inbox",
        "bpm_task_id": "xxx",
        "assignee": "审批人ID",
        "initiator_name": "发起人",
        "process_definition_key": "xxx",
        "duration_desc": "2小时"
      }
    ],
    "total": 3,
    "source": "inbox"
  }
}
```

### 数据来源

| 来源 | 外部 API | 方法 | 说明 |
|------|---------|------|------|
| `inbox` | `{PERFORMANCE_SYSTEM_URL}/bpmc/action/task/inboxTasks` | POST | BPM 待办收件箱 |

> **分页**：支持 `?page=1&rows=20`，"全部"分类做后端分页，其余分类整组返回。
> **分类**：按 `processDefinitionKey` + `businessPattern` 归类为 绩效目标申报/事前/事中/事后，非绩效任务被过滤。

### 错误响应

```json
{
  "code": -1,
  "message": "未获取到用户信息，返回空列表",
  "data": null
}
```

---

## 3. 全局搜索（意图分流）

自然语言输入 → 意图识别 → 返回分流结果。采用分层识别：**已选意图短路 → 高置信方向信号短路（操作手册/位置/概念/数据）→ 候选收集（多义则二次追问）→ 单候选直分流 → LLM 兜底**，LLM 不可用时降级关键词匹配。

> **架构（v3 起）**：`/search` 是**纯意图分流器**——只分类不生成回答。落库规则见下方「intent 取值说明」：navigation/recent_projects/policy/cooperation 会**落库会话历史并返回 conversation_id**；**qa 与 query 不落库**（qa 由 `/qa/stream` 落库；query 走 `/query/projects` 纯查询、不落库），disambiguation 也不落库。
>
> **v6 新增**：① `cooperation`（协同操作手册）意图；② `disambiguation`（意图歧义二次追问）——一次输入命中多个意图族时返回候选列表由用户点选；③ 请求新增 `chosen_*` 字段承载用户点选结果。

### 请求

```
POST /api/v1/guide/search
Content-Type: application/json
```

```json
{
  "query": "我想填报绩效目标",
  "user_id": "1696097681761374208",
  "conversation_id": "",
  "request_id": ""
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `query` | string | **是** | 用户自然语言输入 |
| `user_id` | string | 否 | 用户 ID |
| `conversation_id` | string | 否 | 当前 digital_human 会话 ID，传入则续接该会话 |
| `request_id` | string | 否 | 幂等 ID，服务端自动生成 |
| `chosen_intent` | string | 否 | **二次追问回传**：用户从 `disambiguation` 候选中点选的意图，非空则跳过检测直接按此分流 |
| `chosen_sub_intent` | string | 否 | 选定意图的子类型（候选里的 `sub_intent` 原样回传） |
| `chosen_route_id` | string | 否 | 选定为 `navigation` 时的路由 ID（候选里的 `route_id` 回传） |
| `chosen_keyword` | string | 否 | 选定为 `cooperation` 时的协同知识库检索词（候选里的 `keyword` 回传） |

> **认证头**：服务端对转发到知识库/外部系统的请求固定注入 `Tenantid=1`，并透传请求头中的 `token`（未带则不补）。前端正常带 `Uapaccesstoken` 等认证头即可。

### 响应 — 页面导航

当识别为用户想跳转到某个页面时返回。**非 qa 意图会落库会话并返回 conversation_id**。

```json
{
  "code": 0,
  "message": "意图识别成功",
  "data": {
    "intent": "navigation",
    "sub_intent": "target_task_list",
    "confidence": 0.95,
    "route": {
      "id": "target_list",
      "title": "目标申报评价任务列表",
      "url": "/performance/commission/target",
      "params": {}
    },
    "guide_stage": "target_list",
    "description": "为您跳转到目标申报评价任务列表页",
    "conversation_id": "1234567890123456789",
    "request_id": "req-abc123"
  }
}
```

> **`guide_stage`**：前端可根据此值判断应启动哪个业务类型的操作指引场景。值为 `null` 时表示该页面暂无操作指引场景。

### 响应 — 近期项目

v3 新增意图。非 qa 意图，落库会话并返回 `conversation_id`。

```json
{
  "code": 0,
  "message": "检测为近期项目查询意图",
  "data": {
    "intent": "recent_projects",
    "sub_intent": "recent_projects",
    "confidence": 0.9,
    "description": "为您展示近期参与的绩效项目列表。",
    "action": "show_recent_projects",
    "conversation_id": "1234567890123456789"
  }
}
```

> **前端行为**：收到此意图后直接调用 `openRecentProjects()` 弹出近期项目列表。

### 响应 — 知识问答

**仅返回标识，不含答案**。前端收到后调用 `POST /qa/stream` 取得流式回答。**不落库会话**（由 `/qa/stream` 负责落库）。

```json
{
  "code": 0,
  "message": "检测为知识问答意图，请使用 /qa 接口",
  "data": {
    "intent": "qa",
    "sub_intent": "general",
    "confidence": 0.88,
    "description": "检测为知识问答，请调用 /qa 接口获取回答",
    "action": "call_qa_api"
  }
}
```

> **前端行为**：收到 `action: "call_qa_api"` 后调用 `POST /qa/stream` 获取流式回答（见 §4）。

### 响应 — 数据查询意图

**仅返回标识，不落库**。识别为「问某个绩效任务的数据」（如"2026年乐山基础建设事后评价任务进度"），前端收到后调用 `POST /query/projects`（§5.2）在系统内定位匹配的绩效任务，返回「任务基本信息 + 关键指标」的卡片数据，**前端渲染成项目卡片，不经大模型分析**。

```json
{
  "code": 0,
  "message": "检测为数据查询意图",
  "data": {
    "intent": "query",
    "sub_intent": "smart_query",
    "confidence": 0.85,
    "description": "检测为数据查询问题，正在为您查询相关绩效任务。",
    "action": "call_smart_query_api"
  }
}
```

> **前端行为**：收到 `action: "call_smart_query_api"` 后，用原始 `query` 调 `POST /query/projects`（普通 JSON，见 §5.2），`project_ids` 传空数组由后端自动定位任务；返回后渲染成项目卡片（含 [提问]/[问数] 按钮）。
>
> **与 §5 `/query/redirect` 的关系**：项目卡片 [问数] 按钮走 §5 外部系统跳转（外部系统再回调 §5.1 `/query/analyze` 取分析）；本 `query` 意图只走 §5.2 系统内卡片查询，三者隔离。
>
> ⚠️ **不落库的原因**：`/query/projects` 是纯数据查询、本身不落库，故 `/search` 此处也不落库（保持与 qa 一致的分流器语义，无 `request_id` 幂等负担）。

### 响应 — 文件查找意图（file_query）

**仅返回标识 + 拆出的文件名，不落库**。识别为「找某个文件」（如"我想查找2025年预算管理办法文件"、"查招标文件的第三段内容"）时命中，后端从输入中拆出**完整文件名** `file_name`，前端据此调用 §10 `/file/reverse-locate` 按文件名反查、一步返回权限过滤后的文件列表。

```json
{
  "code": 0,
  "message": "检测为文件查询意图",
  "data": {
    "intent": "file_query",
    "sub_intent": "file_locate",
    "confidence": 0.9,
    "file_name": "2025年预算管理办法",
    "description": "正在为您查找文件「2025年预算管理办法」相关内容。",
    "action": "call_file_query_api"
  }
}
```

> **命中条件**：输入含「文件」二字，且带 位置信号（在哪里/在哪/怎么找）、内容信号（内容/段落/第x段）或查询动作词（查询/查找/找/想找/打开/下载…），或本身以「…文件」结尾。排除 新建/上传/删除 等操作性表述；文件名含「文件管理/菜单」等则让位页面导航。此意图在 `/search` 分流顺序中位于「位置查询」之前短路。
>
> **前端行为**：收到 `intent:"file_query"` 后，用拆出的 `file_name` 调 §10 `/file/reverse-locate`，直接渲染返回的文件列表（已按用户数据权限过滤、相似度倒排，每个文件带所属绩效任务名）。

### 响应 — 政策咨询意图

非 qa 意图，落库会话。

```json
{
  "code": 0,
  "message": "检测为政策咨询意图，请使用政策助手系统",
  "data": {
    "intent": "policy",
    "sub_intent": "policy_inquiry",
    "confidence": 0.82,
    "description": "政策法规咨询请使用 [政策助手] 按钮，将跳转到政策法规问答系统。",
    "action": "redirect_to_policy_qa",
    "conversation_id": "1234567890123456789"
  }
}
```

### 响应 — 协同操作手册意图（cooperation）

用户明确说「xxx操作手册」（如"事后评价操作手册""项目负责人操作手册"）时命中，直接检索固定协同知识库（`lib_id=2064627004150509569`）并**内联返回手册内容**（`items`），落库会话。也可由 `disambiguation` 点选「查看操作手册」后经 `chosen_intent=cooperation` 触发。

```json
{
  "code": 0,
  "message": "检测为操作手册意图",
  "data": {
    "intent": "cooperation",
    "sub_intent": "operation_manual",
    "confidence": 0.95,
    "description": "为您展示「事后评价」的操作手册指引。",
    "action": "show_cooperation_guide",
    "userRole": "事后评价",
    "total": 2,
    "items": [
      { "title": "事后评价操作手册", "content": "第一步……", "score": 0.87, "metadata": {} }
    ],
    "conversation_id": "1234567890123456789"
  }
}
```

> **前端行为**：直接渲染 `items`（title + content）到聊天气泡，无需再发请求。检索词来自「xxx操作手册」正则提取的 `xxx`，或点选候选时回传的 `chosen_keyword`。

### 响应 — 意图歧义二次追问（disambiguation）

一次输入同时命中**多个不同意图族**（如"事后评价怎么操作" = 进列表页看操作指引 / 查操作手册）时返回，**不落库**。前端渲染 `candidates` 为按钮，用户点选后带对应 `chosen_*` 字段重发 `/search`（复用原 `request_id`）完成分流。

```json
{
  "code": 0,
  "message": "需要进一步明确意图",
  "data": {
    "intent": "disambiguation",
    "description": "您的问题可能涉及多个方面，请选择您想做的：",
    "candidates": [
      { "intent": "navigation", "sub_intent": "after_task_list", "label": "🧭 进入事后评价评价任务列表并查看操作指引", "route_id": "after_list" },
      { "intent": "cooperation", "sub_intent": "operation_guide", "label": "📖 查看事后评价评价任务列表操作手册", "keyword": "事后评价评价任务列表" }
    ]
  }
}
```

| 候选字段 | 说明 | 点选后回传到 |
|---------|------|------------|
| `intent` | 候选意图 | `chosen_intent` |
| `sub_intent` | 子类型 | `chosen_sub_intent` |
| `label` | 按钮展示文案 | —（仅展示） |
| `route_id` | 仅 navigation 候选有 | `chosen_route_id` |
| `keyword` | 仅 cooperation 候选有（协同库检索词） | `chosen_keyword` |

> **触发原则**：只有「明确主意图」缺失、多个检测器各自独立命中才追问。明说"操作手册"→直接给手册；裸业务词"事后评价"→直接导航；"怎么操作/操作步骤"这类模糊操作诉求才弹选项。

### 意图识别流程

```
用户输入
    │
    ▼
⓪ chosen_intent 非空？（用户已从 disambiguation 候选点选）
    └── 是 → 跳过检测，按 chosen_intent 直接分流 → 返回
    │（否 ↓）
    ▼
① 高置信方向信号短路（意图明确，不追问）
    ├── "xxx操作手册"           → cooperation（检索协同库，内联返回 items）→ 落库 → 返回
    ├── 位置查询("xxx在哪里")     → navigation → 落库 → 返回
    ├── 概念问句("xxx是什么/含义") → qa（不落库）→ 返回
    └── 数据查询(业务词+数值/维度信号 或 评价类型词) → query（不落库）→ 返回
    │（均未命中 ↓）
    ▼
② 候选收集（无明确方向信号时，汇总各检测器独立命中的意图族）
    · 操作诉求("xxx怎么操作/操作步骤") + 命中业务页 → navigation + cooperation 两候选
    · 关键词命中 route → navigation 候选
    · "我的项目/参与了哪些" → recent_projects 候选
    ├── ≥2 个不同意图族 → disambiguation（不落库）→ 返回候选列表待点选
    ├── 恰好 1 个 → 直接分流（navigation 需关键词置信度 ≥0.8，否则 ↓）
    └── 0 个 ↓
    ▼
③ LLM 意图识别（禁止硬猜导航；项目名→query；无法归类→qa）
    ├── navigation/recent_projects/policy → 落库 → 返回
    ├── query → 只返回标识（前端调 /query/projects 取任务卡片）；qa → 只返回标识，不落库
    └── LLM 不可用 → 有 navigation 候选则用之，否则降级 qa 标识
```

> **概念 vs 操作**：`是什么/含义/定义/概念` 归 qa（要知识解释）；`怎么操作/操作步骤/怎么做/怎么填` 是"要怎么做"的操作诉求，走候选收集弹选项（进列表页看指引 / 查操作手册），**不再**当成概念问答。

### `intent` 取值说明

| intent | 说明 | 是否落库 |
|--------|------|----------|
| `navigation` | 页面导航，含 `route` + `guide_stage` | ✅ 落库 digital_human 会话 |
| `recent_projects` | 查看近期项目，前端弹窗 | ✅ 落库 digital_human 会话 |
| `cooperation` | 协同操作手册，内联返回 `items` | ✅ 落库 digital_human 会话 |
| `policy` | 政策咨询，引导使用政策助手 | ✅ 落库 digital_human 会话 |
| `query` | 数据查询，前端调 `/query/projects` 返回任务卡片（`action=call_smart_query_api`） | ❌ 不落库（`/query/projects` 为纯查询） |
| `file_query` | 文件查找，含拆出的 `file_name`，前端调 §10 `/file/reverse-locate` 一步出文件 | ❌ 不落库（纯查询不落库） |
| `qa` | 知识问答，前端调 `/qa/stream` | ❌ 不落库，由 `/qa/stream` 负责 |
| `disambiguation` | 意图歧义，返回 `candidates` 待用户点选 | ❌ 不落库，点选后由 `chosen_intent` 请求落库 |

### 全局搜索联动操作指引

当意图为 `navigation` 时，响应中 `guide_stage` 字段指示前端应启动哪个业务类型的操作指引预制流程：

| guide_stage | 对应操作指引场景 | 步骤数 |
|-------------|----------------|--------|
| `target_list` | 绩效目标申报完整流程 | 15 步 |
| `before_list` | 事前评估完整流程 | 16 步 |
| `middle_list` | 事中监控完整流程 | 17 步 |
| `after_list` | 事后评价完整流程 | 16 步 |

---

## 4. 知识问答（SSE 流式）

前端收到 `/search` 返回的 `qa` 标识后调用。**负责落库 digital_human 会话历史**。

```
POST /api/v1/guide/qa/stream
Content-Type: application/json
```

```json
{
  "query": "什么是预算绩效管理",
  "user_id": "1696097681761374208",
  "conversation_id": "",
  "request_id": ""
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `query` | string | **是** | 用户问题 |
| `user_id` | string | 否 | 用户 ID |
| `conversation_id` | string | 否 | 续接已有 digital_human 会话；为空则新建 |
| `request_id` | string | 否 | 幂等 ID |

响应为 **SSE 流式**（`text/event-stream`）。

**事件协议**：

| event | data | 说明 |
|-------|------|------|
| `meta` | `{"conversation_id","turn_no","replayed"}` | 会话元信息 |
| `delta` | `{"content":"预"}` → `{"content":"算"}` … | 逐字增量，可多次 |
| `done` | `{"conversation_id","request_id","answer","source"}` | 完成 |
| `error` | `{"message":"…"}` | 异常 |

**前端对接示例**：

```javascript
fetch('/api/v1/guide/qa/stream', {
  method: 'POST',
  headers: {'Content-Type':'application/json'},
  body: JSON.stringify({query, user_id, conversation_id, request_id})
}).then(async res => {
  const reader = res.body.getReader();
  const decoder = new TextDecoder();
  let buf = '', answer = '';
  while (true) {
    const {done, value} = await reader.read();
    if (done) break;
    buf += decoder.decode(value, {stream:true});
    // 解析 SSE 行: event:delta\ndata:{"content":"xxx"}
    // 逐行追加 answer 并渲染气泡
    const lines = buf.split('\n');
    buf = lines.pop();
    for (const line of lines) {
      if (line.startsWith('data:')) {
        const data = JSON.parse(line.slice(5).trim());
        if (data.content) answer += data.content;  // delta
        if (data.conversation_id) saveCid(data.conversation_id); // done
      }
    }
    updateBubble(escapeHtml(answer));
  }
});
```

---

## 5. 智能问数跳转

构造外部智能问数系统的跳转 URL，携带项目上下文参数。**本接口仅返回跳转 URL，不承载数据查询逻辑；查数、算指标、出结论均由外部智能问数系统完成。**

支持两种场景（已合并原「跨项目智能问数分析」，跨项目问数不再在本系统内做 SSE 分析，统一跳转外部系统）：

- **单项目**：带 `commission_task_id`（项目卡片【问数】按钮触发），从达梦库补全 `taskName`/`taskType` 作为上下文。
- **跨项目 / 自然语言**：不带 `commission_task_id`，用 `query`（可选 `project_ids`）跳转外部系统由其处理。

> `commission_task_id` 与 `query` 至少提供其一。

### 请求

```
POST /api/v1/guide/query/redirect
Content-Type: application/json
```

```json
{
  "commission_task_id": "2026906953246052352",
  "query": "完成了多少产出指标？",
  "user_id": "1696097681761374208"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `commission_task_id` | string | 条件 | 评价任务 ID（单项目场景必填；与 `query` 至少其一） |
| `project_ids` | string[] | 否 | 项目 ID 列表（跨项目场景，透传到外部系统） |
| `query` | string | 条件 | 用户查询问题（透传到外部系统；与 `commission_task_id` 至少其一） |
| `user_id` | string | 否 | 用户 ID |

### 响应

```json
{
  "code": 0,
  "message": "智能问数跳转链接",
  "data": {
    "redirect_url": "https://smart-query.example.com?commissionTaskId=2026906953246052352&taskName=XX基金绩效评价&taskType=2&userId=1696097681761374208&query=完成了多少产出指标？",
    "external_system_name": "智能问数系统",
    "open_in_new_tab": true
  }
}
```

### 携带参数

| 参数 | 来源 | 说明 |
|------|------|------|
| `commissionTaskId` | 请求参数 | 评价任务 ID（**单项目**场景携带） |
| `taskName` | 达梦数据库查询 | 任务名称（**单项目**场景，从 `b_project_commission_task` 表获取） |
| `taskType` | 达梦数据库查询 | 任务类型（**单项目**场景，1/2/3/5） |
| `projectIds` | 请求参数 | 项目 ID 列表，逗号分隔（**跨项目**场景，无 `commission_task_id` 且传了 `project_ids` 时携带） |
| `userId` | 请求参数 | 用户 ID |
| `query` | 请求参数（可选） | 用户查询问题 |

### 前置条件

- 环境变量 `SMART_QUERY_SYSTEM_URL` 必须配置，否则返回：`"智能问数系统暂未配置，请联系管理员"`

### 前端使用

```javascript
async function openSmartQuery({ commissionTaskId, query }) {
  const res = await fetch('/api/v1/guide/query/redirect', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      // 单项目：传 commission_task_id；跨项目/自然语言：传 query（可选 project_ids）
      commission_task_id: commissionTaskId || '',
      query: query || '',
      user_id: CURRENT_USER_ID
    })
  });
  const { data } = await res.json();
  if (data && data.redirect_url) {
    window.open(data.redirect_url, data.open_in_new_tab === false ? '_self' : '_blank');
  }
}
```

---

## 5.1 跨项目智能问数分析（SSE 流式）

在本系统内完成跨项目/单项目的数据查询与大模型分析，返回文字结论与图表。支持自然语言提问、多轮追问、图表输出。

> **调用方**：§5 `/query/redirect` 只负责把前端跳转到**外部智能问数系统**（展示层）；外部系统真正的数据查询与分析请求打回本接口。即：前端跳转 → 外部系统 UI → 调用本后端 `/query/analyze` 拿分析结果。

**与 §5 `/query/redirect` 的区别**：§5 是给前端拼跳转 URL；本接口在系统内查数据、算指标、出分析结论（外部系统的数据/分析来源）。

### 数据来源与权限

- **项目定位**：调用外部绩效系统 `pageList` API（`/jx/web/api/v1/projectCommissionTask/pageList`），透传前端认证头（`Uapaccesstoken`/`Uaprefreshtoken`）实现数据权限隔离——用户只能分析有权限的项目。
- **明细数据**：达梦库按维度加载，**三层下钻聚合**——任务基础信息层查不到时，自动下钻到「评价子任务」「子项目执行指标」「预算执行指标/执行率/业务资金明细」等明细层做求和/平均/执行率/占比/排名等计算。
- **数据脱敏**：回答中的项目ID、数据库字段名会被过滤，统一用项目名称和中文标签呈现。

### 处理流程

```
意图提取(1次LLM: 范围/类型/关键词/维度)
  → pageList API 定位项目(权限控制)
  → 方案B: 达梦按维度加载模块数据 → LLM分析
     └ 无数据时降级方案C: LLM生成SQL → 安全执行 → 分析
  → SSE 流式返回 + 落库会话
```

### 请求

```
POST /api/v1/guide/query/analyze
Content-Type: application/json
（须携带 Uapaccesstoken / Uaprefreshtoken 请求头）
```

```json
{
  "query": "所有事后评价项目的预算执行率排名",
  "project_ids": [],
  "user_id": "1696097681761374208",
  "conversation_id": "",
  "request_id": ""
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `query` | string | **是** | 自然语言问题 |
| `project_ids` | string[] | 否 | 指定项目 ID 列表；为空时由 LLM 从 query 分析并经 pageList 定位 |
| `user_id` | string | 否 | 用户 ID |
| `conversation_id` | string | 否 | 续接会话（`digital_human` 类型），支持多轮追问；为空则新建 |
| `request_id` | string | 否 | 幂等 ID；同 ID 重复请求直接返回已有回答 |

> **意图识别**：LLM 一次性（单次调用）判断查询范围、任务类型（可多选）、定位关键词、分析维度。
> - 单项目："乐山2026年财政绩效委托项目的点位得分" → 精确定位一个项目
> - 多项目单类型："所有事后评价项目的成果统计" → `commissionTaskType=2`，不限条数
> - 多项目多类型："对比事前和事中的得分" → `commissionTaskType=1,5`（逗号分隔多值）
> - 全阶段："所有阶段的资金情况" → 不传 `commissionTaskType`，查全部类型
> - 追问：带 `conversation_id` 时，"再按金额排序" 会结合历史理解为完整意图。
>
> **任务类型编码**：事前评估=1、事后评价=2、目标申报=3、事中监控=5（见附录 A）。

### 响应（SSE 流式）

```
event: meta
data: {"conversation_id":"1871...","request_id":"...","replayed":false}

event: delta
data: {"content":"## 预算执行率排名\n\n"}

event: delta
data: {"content":"根据分析，各项目执行率如下..."}

event: done
data: {"answer":"完整回答","plan":"B","dimension":"budget","projects_analyzed":8,"conversation_id":"1871...","request_id":"..."}
```

| 事件 | 字段 | 说明 |
|------|------|------|
| `meta` | `conversation_id` / `request_id` / `replayed` | 会话元信息，首个事件 |
| `delta` | `content` | 流式增量文本（含 Markdown、` ```echarts ` 图表代码块） |
| `done` | `answer` | 完整回答（前端可用流式拼接结果） |
| `done` | `plan` | 数据获取方案：`B`(模块加载)/`C`(SQL生成)/`fallback`(未定位到项目) |
| `done` | `dimension` | 分析维度：point_scores/quality_control/budget/problems/progress/reports/overview |
| `done` | `projects_analyzed` | 实际分析的项目数 |
| `error` | `message` | 错误信息 |

### 特殊场景

**① 未找到项目**：`plan="fallback"`，`answer` 提示确认项目名称/类型。

**② 单项目意图命中多条**：`done` 事件带 `candidates` 候选列表，提示用户明确指定：

```
event: done
data: {"answer":"查询到 3 条相似的绩效项目：...","plan":"fallback","dimension":"","projects_analyzed":0,
       "candidates":[{"id":"...","name":"XX项目","type":"2"}, ...]}
```

**③ 幂等重放**：相同 `request_id` 重复请求，直接返回已有回答，`meta` 与 `done` 带 `replayed:true`：

```
event: meta
data: {"conversation_id":"1871...","request_id":"...","replayed":true}

event: done
data: {"answer":"已有回答","plan":"replayed","conversation_id":"1871...","replayed":true}
```

**④ 图表**：分析结果可含 ` ```echarts ` 代码块，前端解析 JSON 后用 ECharts 渲染。ECharts 配置的 key 为英文，值可为中文。

**⑤ 会话准备失败降级**：若会话服务异常，接口仍会正常分析并返回结果，只是 `conversation_id` 为空（不影响本次问答，仅无法多轮追问）。

### 前端使用

```javascript
let cid = '';  // 保存会话ID用于追问
async function ask(query) {
  const res = await fetch('/api/v1/guide/query/analyze', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...API_HEADERS },
    body: JSON.stringify({ query, conversation_id: cid })
  });
  const reader = res.body.getReader();
  const decoder = new TextDecoder();
  let answer = '';
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    for (const block of decoder.decode(value).split('\n\n')) {
      const evt = /event:\s*(\w+)/.exec(block)?.[1];
      const dataMatch = /data:\s*(.+)/.exec(block);
      if (!dataMatch) continue;
      const data = JSON.parse(dataMatch[1]);
      if (evt === 'meta' && data.conversation_id) cid = data.conversation_id;
      if (evt === 'delta') { answer += data.content; renderMarkdown(answer); }
      if (evt === 'done') renderCharts();  // 渲染 echarts 代码块
    }
  }
}
```

---

## 5.2 数据查询任务卡片（/query/projects）

`/search` 意图分流命中 `query`（数据查询）后，前端用原始 `query` 调本接口：系统内定位匹配的绩效任务，加载「基本信息 + 关键指标 + 评价子任务/点位明细」，**结构化返回给前端渲染项目卡片，不经大模型分析**。任务若存在点位数据则一并查出（子任务与点位都展示）。

**与 §5.1 `/query/analyze` 的区别**：本接口只**定位任务 + 算关键指标**并返回结构化数据（普通 JSON，前端渲染卡片）；§5.1 是**大模型流式分析**出文字结论（SSE）。二者完全隔离，`query` 意图只走本接口。

### 数据来源与权限

- **任务定位**：调用外部绩效系统 `pageList` API，透传前端认证头（`Uapaccesstoken`/`Uaprefreshtoken`）做数据权限隔离——用户只能看到有权限的任务。
- **关键指标 + 子任务/点位明细**：达梦库按分析维度加载模块（并始终并入「评价子任务、评价点位」）后聚合计算并抽取明细（仅为前 12 张卡片查库，避免大批量拖慢）。整体防御式，失败时卡片仍返回基本信息。

### 请求

```
POST /api/v1/guide/query/projects
Content-Type: application/json
（须携带 Uapaccesstoken / Uaprefreshtoken 请求头）
```

```json
{
  "query": "所有事后评价项目",
  "project_ids": [],
  "user_id": "1696097681761374208"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `query` | string | **是** | 自然语言问题（用于提取任务类型/名称关键词/分析维度） |
| `project_ids` | string[] | 否 | 指定任务 ID 列表；为空时由 `query` 经 pageList 定位 |
| `user_id` | string | 否 | 用户 ID |

### 响应

```json
{
  "code": 0,
  "message": "查询到相关绩效任务",
  "data": {
    "projects": [
      {
        "commissionTaskId": "2079773087175282688",
        "title": "乐山2026年度财政绩效委托项目",
        "taskType": 2,
        "taskTypeName": "事后评价",
        "status": "in_progress",
        "statusText": "进行中",
        "deadline": "2026-08-19",
        "startDate": "2026-06-01",
        "chargePersonName": "张三",
        "metrics": [
          { "label": "平均得分", "value": "85.6" },
          { "label": "点位数", "value": "12" }
        ],
        "subtaskCount": 5,
        "subtasks": [
          { "name": "子任务A", "statusText": "已完成", "pointName": "XX点位", "score": "88.5" },
          { "name": "子任务B", "statusText": "进行中" }
        ],
        "pointCount": 12,
        "points": [
          { "name": "XX点位", "statusText": "已完成" },
          { "name": "YY点位", "statusText": "进行中" }
        ]
      }
    ],
    "total": 35,
    "shown": 12,
    "metricLoaded": 12,
    "scope": "multiple",
    "taskTypes": ["事后评价"],
    "keywords": [],
    "dimension": "point_scores",
    "dimensionName": "点位得分"
  }
}
```

| 字段 | 说明 |
|------|------|
| `projects` | 任务卡片列表；`commissionTaskId` 为字符串（避免 JS 精度丢失） |
| `projects[].metrics` | 按 `dimension` 算的 1~2 个关键指标（`label`/`value`）；无数据时为空数组 |
| `projects[].subtasks` / `subtaskCount` | 评价子任务明细（`name`/`statusText`，可选 `pointName`/`score`）与总数；最多返回 20 条 |
| `projects[].points` / `pointCount` | 评价点位明细（`name`/`statusText`）与总数；**该任务无点位时为空数组**，最多返回 20 条 |
| `total` | 命中任务总数 |
| `shown` | 实际返回的卡片数（上限 12） |
| `metricLoaded` | 成功加载指标的卡片数 |
| `scope` | `single`（单任务）/ `multiple`（多任务汇总） |
| `taskTypes` | 从 query 提取的任务类型（可多个） |
| `keywords` | 从 query 提取的任务名关键词 |
| `dimension` / `dimensionName` | 分析维度编码与中文名（决定 metrics 内容） |

> **维度 → 关键指标**：`budget`=预算总额+执行率 / `point_scores`=平均得分+点位数 / `quality_control`=问题数+评价要点 / `problems`=问题数+薄弱环节 / `progress`=子任务+点位数 / `reports`=报告数+报告任务 / `overview`=子任务+问题数。

### 无结果

命中 0 条时 `projects` 为空数组，`message` 给出提示：

```json
{
  "code": 0,
  "message": "未找到匹配 “事后评价” 的绩效任务，请确认任务名称或类型后重试。",
  "data": { "projects": [], "total": 0, "shown": 0, "message": "未找到匹配 “事后评价” 的绩效任务，请确认任务名称或类型后重试。", "...": "..." }
}
```

### 前端使用

```javascript
const res = await fetch('/api/v1/guide/query/projects', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json', ...API_HEADERS },
  body: JSON.stringify({ query, project_ids: [], user_id: UID })
}).then(r => r.json());
if (res.code === 0) renderQueryProjectCards(res.data);  // 渲染项目卡片（提问/问数按钮）
```

---

## 6. 项目助手问答

根据用户选择的四阶段之一，结合项目全量上下文，调用 LLM 生成深度回答。支持多轮会话与历史记录。

> **两种形态**：`POST /assistant/project/stream`（**SSE 流式，前端首页默认调用**，逐字增量推送）与
> `POST /assistant/project`（非流式，一次性返回完整回答）。二者请求体、会话隔离规则、阶段上下文完全一致，
> 仅回答的传输方式不同。新接入建议使用流式版本。
>
> ⚠ **前端未调用**：绩效平台首页仅调用**流式** `/assistant/project/stream`（§6.1）；非流式
> `/assistant/project` 后端保留（供其他端/未来使用），当前前端未调用。

### 请求

```
POST /api/v1/guide/assistant/project        # 非流式
POST /api/v1/guide/assistant/project/stream  # SSE 流式
Content-Type: application/json
```

```json
{
  "stage": "post_evaluation",
  "commission_task_id": "2079773087175282688",
  "project_name": "XX基金绩效评价",
  "query": "如何撰写绩效自评报告？",
  "user_id": "1696097681761374208",
  "conversation_id": "",
  "request_id": ""
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `stage` | string | **是** | 阶段编码（见附录 B） |
| `commission_task_id` | string | **是** | 评价任务 ID（字符串，避免 JS 精度丢失） |
| `project_name` | string | 否 | 项目名称，新会话时作为会话标题 |
| `query` | string | **是** | 用户问题 |
| `user_id` | string | 否 | 用户 ID |
| `conversation_id` | string | 否 | 续接已有**项目会话**（project 类型）；为空则新建 |
| `request_id` | string | 否 | 幂等 ID |

> **会话隔离**：传入 `conversation_id` 时会校验归属、类型（必须为 `project`）、项目 ID 和阶段一致性，不匹配直接返回错误。

### 响应

```json
{
  "code": 0,
  "message": "项目助手回答成功",
  "data": {
    "stage": "post_evaluation",
    "stage_name": "事后评价",
    "commission_task_id": "2026906953246052352",
    "answer": "撰写绩效自评报告时，建议按以下结构组织内容：\n\n## 一、项目基本情况\n...",
    "references": [],
    "suggestions": [
      "绩效自评报告撰写指南",
      "如何对比目标与实际完成情况？",
      "项目经验总结与改进建议"
    ],
    "source": "llm"
  }
}
```

| 字段 | 说明 |
|------|------|
| `answer` | LLM 生成的回答（Markdown 格式） |
| `references` | 知识库引用（预留） |
| `suggestions` | 建议追问列表（按阶段预设） |
| `source` | `"llm"` 正常生成，`"fallback"` LLM 不可用时的降级消息 |

### 降级场景

LLM 调用失败时，`source` 为 `"fallback"`，返回基本提示信息：

```json
{
  "answer": "[降级] 您正在事后评价阶段查询项目 2026906953246052352。完整的智能回答将在 LLM 恢复后提供。",
  "source": "fallback"
}
```

### 流式响应（`/assistant/project/stream`）

响应为 **SSE 流式**（`text/event-stream`）。

**事件协议**：

| event | data | 说明 |
|-------|------|------|
| `meta` | `{"conversation_id","stage","stage_name","commission_task_id","turn_no","request_id","replayed"}` | 会话元信息，首个事件 |
| `delta` | `{"content":"撰"}` → `{"content":"写"}` … | LLM 回答逐字增量原文，可多次 |
| `done` | `{"conversation_id","request_id","answer","source","suggestions","replayed"}` | 完成，`answer` 为**脱敏后完整回答**，前端以此为准 |
| `error` | `{"message":"…"}` | 异常（含会话隔离校验失败） |

> **脱敏与落库时机**：`delta` 推送 LLM 原始增量（响应优先，不逐块脱敏）；流结束后对完整文本统一做
> ID/字段名脱敏（`_clean_ids_from_answer`），脱敏结果**落库**并随 `done.answer` 下发。
> 前端应在 `done` 到达时用 `done.answer` 覆盖已累积的 delta 文本，保证展示的是脱敏版本。
> `stage` 非法 / `query` 为空 / `commission_task_id` 为空时，直接返回普通 JSON 错误（不进入 SSE 流）。

**前端对接示例**：

```javascript
fetch('/api/v1/guide/assistant/project/stream', {
  method: 'POST',
  headers: {'Content-Type':'application/json'},
  body: JSON.stringify({stage, commission_task_id, project_name, query, user_id, conversation_id, request_id})
}).then(async res => {
  const reader = res.body.getReader();
  const decoder = new TextDecoder();
  let buf = '', answer = '';
  while (true) {
    const {done, value} = await reader.read();
    if (done) break;
    buf += decoder.decode(value, {stream:true});
    const parts = buf.split('\n'); buf = parts.pop();
    for (let i = 0; i < parts.length; i++) {
      let evt = '', data = '';
      if (parts[i].startsWith('event:')) { evt = parts[i].slice(6).trim(); if (parts[i+1]?.startsWith('data:')) { data = parts[i+1].slice(5).trim(); i++; } }
      else if (parts[i].startsWith('data:')) data = parts[i].slice(5).trim();
      if (!data) continue;
      const obj = JSON.parse(data);
      if (evt === 'meta' && obj.conversation_id) saveCid(obj.conversation_id);
      if (evt === 'delta' && obj.content) { answer += obj.content; updateBubble(escapeHtml(answer)); }
      if (evt === 'done') { answer = obj.answer || answer; updateBubble(escapeHtml(answer)); } // 脱敏版覆盖
    }
  }
});
```

### 项目上下文查询范围（按阶段加载）

项目助手按阶段加载不同的项目数据模块作为 LLM 上下文（`STAGE_MODULES`），减少 token 消耗。

**公共模块**（所有阶段）：辅助配置、项目大组、评价子任务、评价点位、指标评分、问题汇总、评价要点、薄弱环节。

| 阶段 | 额外加载模块 |
|------|-------------|
| `target_declaration` 目标申报 | 下达指标、评价指标目标、评价要求 |
| `pre_evaluation` 事前评估 | 下达指标、方案任务、报告任务、报告 |
| `mid_monitoring` 事中监控 | 监控单位、绩效分析、项目绩效、日报、周报、重大事项 |
| `post_evaluation` 事后评价 | 项目执行指标、评价统计、报告任务、报告、项目关联、总结内容 |

> 每个列表模块最多展示前 10 条，超出部分截断标注 `... 共 N 条（以上展示前 10 条）`。
> **ID 与字段名脱敏**：回答后处理会移除数据库字段名和数字 ID，统一替换为中文标签或"该项目/该点位"。

### 阶段系统提示词

每个阶段有专用的 LLM 系统提示词，详见下表：

| 阶段 | 提示词侧重 |
|------|----------|
| `target_declaration` | 指标设置规范、申报完整性检查、同类参考、可量化指标设定 |
| `pre_evaluation` | 政策解读、事前报告编制、立项必要性、预算合理性、可行性论证 |
| `mid_monitoring` | 进度偏差分析、关键节点提醒、偏离预警、监控报告填写、预算执行率 |
| `post_evaluation` | 自评报告撰写、目标vs实际对比、经验总结、指标体系解读、改进建议 |

### 各阶段建议追问

| 阶段 | 建议追问 |
|------|---------|
| `target_declaration` | "如何设置合理的绩效指标？" / "绩效目标申报需要哪些材料？" / "同类型项目的指标参考" |
| `pre_evaluation` | "事前评估报告如何撰写？" / "项目立项必要性分析要点" / "预算合理性评估方法" |
| `mid_monitoring` | "当前项目执行进度如何？" / "如何识别绩效目标偏离风险？" / "绩效监控报告填写要点" |
| `post_evaluation` | "绩效自评报告撰写指南" / "如何对比目标与实际完成情况？" / "项目经验总结与改进建议" |

---

## 7. 项目助手 · 会话管理

> 前端首页仅使用**会话列表**与**会话消息**两个只读接口用于历史记录回放。

### 7.1 会话列表

```
GET /api/v1/guide/assistant/conversations?user_id=xxx&conversation_type=&limit=50
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `user_id` | string | 否 | 用户 ID（后端从 token 解析） |
| `conversation_type` | string | 否 | 过滤类型：`project` / `digital_human`；不传返回全部 |
| `limit` | int | 否 | 每页条数，默认 50 |

**响应**：

```json
{
  "code": 0,
  "data": {
    "conversations": [
      {
        "id": "1234567890123456789",
        "userId": "1696097681761374208",
        "conversationType": "project",
        "commissionTaskId": "2079773087175282688",
        "taskType": 2,
        "stage": "post_evaluation",
        "title": "XX基金绩效评价",
        "conversationStatus": "active",
        "messageCount": 6,
        "lastMessagePreview": "撰写自评报告时，建议按以下结构…",
        "lastMessageAt": "2026-07-22 15:30:00",
        "createdAt": "2026-07-22 10:00:00",
        "updatedAt": "2026-07-22 15:30:00"
      }
    ],
    "total": 5
  }
}
```

> **`conversationType`**：`project` = 项目问答，`digital_human` = 普通数字人问答。前端据此渲染类型标签，恢复会话时选择对应发送接口。

### 7.2 会话消息

```
GET /api/v1/guide/assistant/conversation/{conversation_id}/messages?user_id=xxx&page=1&rows=20
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `user_id` | string | 否 | **归属校验**：仅返回属于该用户的会话消息，不匹配返回 403 |
| `page` | int | 否 | 页码，默认 1 |
| `rows` | int | 否 | 每页条数，默认 20，最大 200 |

**响应**：

```json
{
  "code": 0,
  "data": {
    "conversation": {"id":"…","conversationType":"project","title":"…"},
    "messages": [
      {
        "id": "msg-1",
        "seqNo": 1,
        "turnNo": 1,
        "role": "USER",
        "content": "如何撰写绩效自评报告？",
        "messageStatus": "COMPLETED",
        "createdAt": "2026-07-22 15:30:00"
      },
      {
        "id": "msg-2",
        "seqNo": 2,
        "turnNo": 1,
        "role": "ASSISTANT",
        "content": "撰写自评报告时，建议按以下结构…",
        "messageStatus": "COMPLETED",
        "answerSource": "LLM",
        "createdAt": "2026-07-22 15:30:05"
      }
    ],
    "total": 85,
    "page": 1,
    "rows": 20,
    "totalPages": 5
  }
}
```

> **前端分页**：每页默认 20 条消息（10 轮问答），前端可按 `totalPages` 渲染分页器，翻页时传 `page` 参数。

> **前端恢复会话**：根据 `conversationType` 恢复 `currentConversationState` —— project 走 `/assistant/project/stream`，digital_human 走 `/search`+`/qa/stream`。

---

## 8. 政策助手跳转

构造外部政策法规问答系统的跳转 URL，携带用户身份参数。**本接口仅返回跳转 URL，不承载政策问答逻辑。**

### 请求

```
POST /api/v1/guide/assistant/policy/redirect
Content-Type: application/json
```

```json
{
  "query": "绩效评价的最新政策有哪些？",
  "user_id": "1696097681761374208"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `query` | string | 否 | 用户政策问题（预留透传） |
| `user_id` | string | 否 | 用户 ID |

### 响应

```json
{
  "code": 0,
  "message": "政策助手跳转链接",
  "data": {
    "redirect_url": "https://policy-qa.example.com?userId=1696097681761374208",
    "external_system_name": "政策法规问答系统",
    "open_in_new_tab": true
  }
}
```

### 携带参数

跳转到外部政策问答系统时，通过 URL query 参数传递用户上下文：

| 参数 | 说明 | 示例 | 当前实现状态 |
|------|------|------|-------------|
| `userId` | 当前登录用户 ID | `1696097681761374208` | ✅ 已实现 |
| `tenantId` | 租户 ID | `xxx` | ⏳ 待实现（需从请求头提取） |

> **待办**：`tenantId` 当前未实现。产品文档定义需携带该参数，但代码 `guide.py` 中的 `policy_assistant_redirect` 仅传递了 `userId`。需从 `request.headers` 中提取 `tenantid` 拼接到跳转 URL。

### 前置条件

- 环境变量 `POLICY_QA_SYSTEM_URL` 必须配置，否则返回：`"政策问答系统暂未配置，请联系管理员"`

### 与全局搜索「知识问答」的区别

| 对比维度 | 政策助手（本接口） | 全局搜索-知识问答 |
|----------|-------------------|-------------------|
| 触发方式 | 主动点击按钮 → 新窗口打开外部系统 | 在搜索框输入问题 |
| 知识范围 | 聚焦政策法规（外部独立系统） | 全域知识库（含操作指引、流程等） |
| 交互深度 | 外部系统独立对话 | 单轮问答为主 |
| 上下文 | 不绑定项目 | 可绑定页面上下文 |

## 9. 协同指引搜索

从固定的协同知识库（`lib_id=2064627004150509569`）检索内容，按传入的**用户角色**筛选，返回与该角色匹配的协同指引条目。

> ⚠ **前端未调用**：绩效平台首页的 `cooperation` 意图内容由 §3 `/search` **内联返回**（`items`），
> 前端不直接调用本 `/cooperation/search` 接口。后端保留供独立调用/其他端使用。

### 请求

```
POST /api/v1/guide/cooperation/search
Content-Type: application/json
```

```json
{
  "token": "eyJhbGciOiJIUzI1NiJ9...",
  "userRole": "项目负责人"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `token` | string | 否 | 认证 token，透传到知识库检索接口做鉴权 |
| `userRole` | string | 是 | 用户角色，作为检索语句并用于结果筛选；为空返回失败 |

> **认证说明**：`token` 会作为请求头透传给知识库检索接口（经 `filter_headers` 仅保留 `token`/`tenantid`）；`tenantId` 当前固定为 `1`。
> **知识库**：`lib_id` 固定为 `2064627004150509569`，检索参数 `top_k=10`、`score_threshold=0.2`。

### 匹配规则

1. 以 `userRole` 作为检索语句调用知识库，召回候选文档。
2. 筛选出 `metadata.title` 或 `content` 中**包含角色名**的文档；若无任何命中，则回退返回全部召回结果。
3. 结果按检索得分 `score` 降序排列。

### 响应

```json
{
  "code": 0,
  "message": "协同指引检索成功",
  "data": {
    "userRole": "项目负责人",
    "total": 2,
    "items": [
      {
        "title": "项目负责人协同指引",
        "content": "项目负责人在事前评估阶段需要……",
        "score": 0.87,
        "metadata": { "title": "项目负责人协同指引" }
      }
    ]
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `userRole` | string | 回显请求的用户角色 |
| `total` | int | 匹配到的条目数 |
| `items` | array | 匹配的指引内容列表，含 `title`/`content`/`score`/`metadata` |

### 失败响应

```json
{ "code": -1, "message": "userRole 不能为空", "data": null }
```

---

## 10. 文件查找 · 文件名反查（一步出文件）

用户在 §3 `/search` 命中 `file_query` 后，前端用拆出的 `file_name` 调本接口。**一步返回权限过滤后的文件列表**，用户不再先选任务。

**流程**：按 `KNOWLEDGE_KN_INFO.TITLE LIKE 文件名` 反查 → `LIB_ID`→库 → `DIR_ID`→子目录 → `ANCESTORS_ID` 首段取根目录（=绩效任务，`PARENT_ID=0`）→ 取根目录 `source_id`(=commission_task_id) 走 **pageList 权限过滤**（只留当前用户有权限的任务）→ 在这些任务的子目录下检索文件名匹配的文件，合并、按文件 ID 去重、相似度倒排。

**关联链路**（达梦实例 `langwell_doc`，schema `LANGWELL-DOC`）：
```
KNOWLEDGE_KN_INFO.TITLE  (匹配文件名)
  ⟶ KNOWLEDGE_LIB_INFO   (l.ID = k.LIB_ID)
  ⟶ KNOWLEDGE_DIR_INFO(子) (d.ID = l.DIR_ID) ── ANCESTORS_ID 首段 ⟶ 根目录(PARENT_ID=0)=绩效任务
  ⟶ 根目录 source_id = commission_task_id  →  pageList 权限过滤
  ⟶ 子目录 → LIB → KN → KNOWLEDGE_FILE_INFO  (取文件名/URL)
```

**权限**：以根目录 `source_id`(=commission_task_id) 走 pageList（透传认证头，按用户数据权限过滤）取用户可见任务全集，与反查结果取交集——只返回用户有权限任务下的文件，防越权。

### 请求

```
POST /api/v1/guide/file/reverse-locate
Content-Type: application/json
（须携带 Uapaccesstoken / Uaprefreshtoken 请求头）
```

```json
{
  "file_name": "点位选取表",
  "user_id": "1696097681761374208"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `file_name` | string | **是** | 用户输入的文件名（按 `KN_INFO.TITLE` 反查） |
| `user_id` | string | 否 | 用户 ID |

### 响应

```json
{
  "code": 0,
  "message": "文件检索成功",
  "data": {
    "file_name": "点位选取表",
    "files": [
      {
        "fileId": "2061325689398173698",
        "fileName": "2026年省级财政重点绩效评价点位选取表（第9包）.xlsx",
        "fileType": "xlsx",
        "url": "/langwell-default/langwell_/2026/06/xx.xlsx",
        "fileSize": 331027,
        "uploadTime": "2026-06-10 13:58:30",
        "taskName": "2026年省级财政重点绩效评价项目",
        "commissionTaskId": "2035904025177624576",
        "score": 0.93
      }
    ],
    "total": 1
  }
}
```

| 字段 | 说明 |
|------|------|
| `files` | 相似度倒排的文件列表（按 `fileId` 去重）；上限 30 条。**已按用户数据权限过滤** |
| `files[].taskName` / `commissionTaskId` | 文件所属绩效任务（供前端展示 + 后续问答） |
| `files[].url` | 文件链接（前端据此打开/下载；相对路径按当前域解析，独立 OSS 需拼接域名） |
| `files[].score` | 文件名与输入的字符串相似度 [0,1] |
| `total` | 返回的文件数 |

> **过滤**：全程 `DEL_FLAG=0`、`TEMP_FLAG=0`、`LIB_STATUS=1`；无认证头一律拒绝（安全默认）。
> **无结果**：`files` 为空数组，`message="未找到匹配的文件（或您无权访问）"`。

### 前端使用

```javascript
// §3 /search 命中 file_query（返回 file_name）→ 直接反查出文件列表
const res = await fetch('/api/v1/guide/file/reverse-locate', {
  method: 'POST', headers: {'Content-Type':'application/json', ...API_HEADERS},
  body: JSON.stringify({file_name: fileName, user_id: UID})
}).then(r => r.json());
if (res.code === 0) renderFileList(res.data.files);  // 文件名 + 所属任务 + 打开链接
```

> **已废弃**（v9 的三步流程）：`/file/locate-task`、`/file/locate-dir`、`/file/search`。其中 `/file/search`（指定任务根目录 + 文件名 → 下钻子目录检索）后端保留未删，供"已知任务查文件"场景复用，前端当前不调用。


## 附录

### 附录 A：taskType 编码

| 编码 | 名称 | 说明 |
|------|------|------|
| `1` | 事前评估 | 项目启动前的绩效评估 |
| `2` | 事后评价 | 项目完成后的绩效评价 |
| `3` | 目标申报 | 项目立项时的绩效目标申报 |
| `5` | 事中监控 | 项目执行中的绩效监控 |

> ⚠ 编码 `4` 为历史保留值，不作业务使用。

### 附录 B：项目助手阶段编码

项目助手问答（§6）与获取阶段项目列表接口使用的四阶段编码：

| 编码 | 名称 | 对应 taskType |
|------|------|-------------|
| `target_declaration` | 绩效目标申报 | 3 |
| `pre_evaluation` | 事前评价 | 1 |
| `mid_monitoring` | 事中监控 | 5 |
| `post_evaluation` | 事后评价 | 2 |

---

## 接口速查表

| # | 方法 | 路径 | 说明 |
|---|------|------|------|
| 1 | GET | `/api/v1/guide/projects/recent` | 近期项目列表（分页） |
| 2 | GET | `/api/v1/guide/projects/todos` | 待办项目列表 |
| 3 | POST | `/api/v1/guide/search` | 全局搜索（意图分流；支持二次追问 disambiguation，qa/query 不落库） |
| 4 | POST | `/api/v1/guide/qa/stream` | 知识问答（SSE 流式） |
| 5 | POST | `/api/v1/guide/query/redirect` | 智能问数跳转（单项目 + 跨项目，前端跳转外部系统） |
| 5.1 | POST | `/api/v1/guide/query/analyze` | 跨项目智能问数分析（SSE 流式，外部系统的数据/分析来源） |
| 5.2 | POST | `/api/v1/guide/query/projects` | 数据查询任务卡片（query 意图分流去向，普通 JSON） |
| 6 | POST | `/api/v1/guide/assistant/project` | 项目助手问答（非流式）⚠ 前端未调用 |
| 6.1 | POST | `/api/v1/guide/assistant/project/stream` | 项目助手问答（SSE 流式，前端默认） |
| 7 | POST | `/api/v1/guide/assistant/policy/redirect` | 政策助手跳转 |
| 8 | GET | `/api/v1/guide/assistant/conversations` | 会话列表 |
| 9 | GET | `/api/v1/guide/assistant/conversation/{id}/messages` | 会话消息 |
| 10 | POST | `/api/v1/guide/cooperation/search` | 协同指引搜索（按角色筛选）⚠ 前端未调用 |
| 11 | POST | `/api/v1/guide/file/reverse-locate` | 文件查找·文件名反查（一步出文件，权限过滤） |

> 共 13 个前端在用接口。`/query/redirect` 负责前端跳转外部智能问数系统（单项目 + 跨项目），外部系统的数据查询与大模型分析仍由本后端 `/query/analyze` 提供；`/assistant/project` 提供流式（前端默认）与非流式两种形态；**文件查找 `/file/reverse-locate` 一步：文件名反查 → 权限过滤 → 直接返回文件列表**。
> **后端保留未在前端调用**：`/file/search`（指定任务查文件，v10 起前端不调用）、`/cooperation/search`（cooperation 走 `/search` 内联）、`/assistant/project`（非流式）。已删除未使用端点：`/intent`、`/qa`(非流式)、`/assistant/conversation/create`、`/assistant/conversation/delete`、`/assistant/projects`、`/page-guide`、`/routes`、`/stages`、`/project/getById`、`/file/locate-task`、`/file/locate-dir`。

---

## 附录 C：前端对接协议

### E.1 会话状态模型

前端维护单一 `currentConversationState`，包含：

| 字段 | 类型 | 说明 |
|------|------|------|
| `conversationType` | `'digital_human'` / `'project'` | 当前会话类型 |
| `conversationId` | string | 持久化会话 ID，首次提问为空 |
| `commissionTaskId` | string | 仅 project 类型 |
| `taskType` | string | 仅 project 类型 |
| `stage` | string | 仅 project 类型 |
| `taskName` | string | 仅 project 类型 |

### E.2 发送路由

```
用户输入
    │
    ├─ conversationType === 'project'
    │   └─ POST /assistant/project/stream（SSE 流式）
    │      （带 stage/commission_task_id/project_name/conversation_id/request_id）
    │      delta 逐字追加，done.answer（脱敏版）覆盖渲染
    │
    └─ conversationType === 'digital_human'
        └─ POST /search
           （带 query/conversation_id/request_id）
           ├─ navigation  → renderSearchResult（路由卡片）
           ├─ recent_projects → openRecentProjects()
           ├─ query → POST /query/projects（普通 JSON，project_ids=[] 后端定位任务）→ 渲染项目卡片
           ├─ policy → 引导按钮
           └─ qa → POST /qa/stream
                  （流式渲染，保存 conversation_id）
```

### E.3 入口切换

| 操作 | 行为 |
|------|------|
| 点击项目卡片"提问" | `conversationType='project'`, `conversationId=''`, 打开面板 |
| 点击"新对话" | `conversationType='digital_human'`, `conversationId=''`, 清空聊天 |
| 点击"×"移除项目引用 | 同"新对话" |
| 恢复历史会话 | 按 `conversationType` 恢复状态，project 走 `/assistant/project/stream`，digital_human 走 `/search` |

### E.4 SSE 流式接收示例

```javascript
async function streamQa(query, conversationId) {
  const res = await fetch('/api/v1/guide/qa/stream', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({query, user_id:UID, conversation_id:conversationId, request_id:genReqId()})
  });
  const reader = res.body.getReader(), decoder = new TextDecoder();
  let buf = '', answer = '';
  while (true) {
    const {done, value} = await reader.read();
    if (done) break;
    buf += decoder.decode(value, {stream: true});
    const lines = buf.split('\n');
    buf = lines.pop();
    for (const line of lines) {
      const m = line.match(/^event:\s*(\w+)/);
      const d = line.match(/^data:\s*(.+)/);
      if (!d) continue;
      const data = JSON.parse(d[1]);
      if (m && m[1] === 'delta' && data.content) {
        answer += data.content;
        updateBubble(answer);  // 逐字渲染
      }
      if (m && m[1] === 'done') {
        saveConversationId(data.conversation_id);
      }
    }
  }
}
```
